#!/bin/sh

# Color aliases
alias \
    grep="grep --color=auto" \
    ip="ip -c" \
    diff="diff --color=auto"

alias \
    mkdir="mkdir -p" \
    ls="eza --git --header" \
    vi="nvim" \
    py="python3" \
    clif="~/rustc_codegen_cranelift/build/cargo.sh" \
    "cd.."="cd .." \
    g="git" \
    open="xdg-open" \
    cp="cp -a"

# Mistypes
alias \
    sl="ls" \
    maek="make"
