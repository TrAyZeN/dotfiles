#!/bin/sh

# Color aliases
alias \
    grep="grep --color=auto" \
    ip="ip -c"

alias \
    mkdir="mkdir -p" \
    ls="exa --git --header" \
    py="python3" \
    clif="~/rustc_codegen_cranelift/build/cargo.sh" \
    "cd.."="cd .."

