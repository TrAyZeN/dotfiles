@import "keys.rasi"

configuration {
  sidebar-mode: true;
  // sort: true;

  timeout {
      action: "kb-cancel";
      delay:  0;
  }

  filebrowser {
      directories-first: true;
      sorting-method:    "name";
  }
}

// @theme "trayzen"
@theme "/usr/share/rofi/themes/gruvbox-dark.rasi"
